/** Default tenant created in 001; internal jobs use this until multi-tenant UX exists. */
export const DEFAULT_TENANT_ID = 1;

/** Minimum length for new account passwords (registration); keep in sync with server validation. */
export const MIN_PASSWORD_LENGTH = 8;

/** Cookie name for session JWT. */
export const SESSION_COOKIE_NAME = 'pachinko_session';
